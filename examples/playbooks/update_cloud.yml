---
- include: disable_os_collect_config.yml
- hosts: undercloud
  name: Disable Undercloud
  tasks:
    - service: name={{ item }} enabled=no state=stopped
      with_items: undercloud-services
- hosts: nova-compute
  name: Disable Overcloud Compute
  tasks:
    - service: name={{ item }} enabled=no state=stopped
      with_items: overcloud-compute-services
- hosts: controller
  name: Disable Overcloud Controller
  tasks:
    - service: name={{ item }} enabled=no state=stopped
      with_items: overcloud-controller-services
- hosts: all
  name: Rebuild and Refresh
  tasks:
    - include: rebuild.yml
    - include: refresh_config.yml
- hosts: controller
  name: Enable Overcloud Controller
  tasks:
    - service: name={{ item }} enabled=yes state=started
      with_items: overcloud-controller-services
- hosts: nova-compute
  name: Enable Overcloud Compute
  tasks:
    - service: name={{ item }} enabled=yes state=started
      with_items: overcloud-compute-services
- hosts: undercloud
  name: Enable Undercloud
  tasks:
    - service: name={{ item }} enabled=yes state=started
      with_items: undercloud-services
